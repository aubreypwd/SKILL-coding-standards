interface:
  display_name: "Aubrey Portwood's Personal Coding Standards"
  short_description: "Strictly validate every code response"
  default_prompt: "Use $aubreypwd-coding-standards for every response containing code, including examples, explanations, diffs, plans, CLI output, and file edits; follow project rules first, then the WordPress baseline and Aubrey's preferences, and do not show code until the compliance gate passes."

policy:
  allow_implicit_invocation: true
